- if @current_user_is_admin
  .admin.dropdown
    = link_to '#', data: { toggle: 'dropdown' }, title: t('actions_and_options'), class: 'glyph' do
      = render partial: 'layouts/campfire'
    .dropdown-menu.dropdown-menu-right
      = link_to new_unit_membership_achievement_path(@unit, @membership, type: 'merit_badge'), class: 'dropdown-item admin', title: t('admin_only') do
        i.fa-tree.fal
        = t('advancement.add.merit_badge')
      = link_to new_unit_membership_achievement_path(@unit, @membership, type: 'non_advancement'), class: 'dropdown-item admin', title: t('admin_only') do
        i.fa-tree.fal
        = t('advancement.add.non_advancement')

