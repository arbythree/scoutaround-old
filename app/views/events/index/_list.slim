table.table
  tr
    th.name      = t('events.table.name')
    th.dates     = t('events.table.dates')
    th.attendees.expendable = t('events.table.attendees')

  = render partial: 'events/index/event', collection: @events
