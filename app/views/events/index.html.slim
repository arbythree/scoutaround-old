- crumbtrail link_to UnitPresenter.unit_display_name(@unit), unit_path(@unit)
- crumbtrail t('events.title')

- content_for :title
  = render 'events/index/utilities'
  h1 = t('events.title')

= render partial: 'events/index/list' unless @view == 'calendar'
= render partial: 'events/index/calendar' if @view == 'calendar'
