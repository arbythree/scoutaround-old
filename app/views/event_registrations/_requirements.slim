.card
  .card-header = t('event_requirements.title')
  .card-body
    ul
      - @event.event_requirements.each do |requirement|
        li = link_to requirement.description, event_requirement_path(requirement)
