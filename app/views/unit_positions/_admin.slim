.admin.dropdown
  = link_to new_unit_unit_position_path(@unit), class: 'glyph' do
    span(data-toggle="tooltip" title="#{t('memberships.administration')}")
      = render partial: 'layouts/campfire'

